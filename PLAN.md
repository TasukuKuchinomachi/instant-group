# グループ分けミニWebアプリ 実装プラン

## 概要
名前を入力し、指定したグループ数に自動で振り分けるシンプルなWebアプリ。リロール(再シャッフル)機能付き。

## 技術スタック
- **Vite** + **React** + **TypeScript**
- **CSS Modules** (追加ライブラリ不要、Vite標準対応)
- 外部UIライブラリなし(ミニアプリなのでシンプルに)

## 画面構成(シングルページ)

### 入力エリア
- 名前入力フィールド(テキスト入力 + 追加ボタン)
- 追加済みの名前リスト(各名前に削除ボタン付き)
- グループ数セレクター(2〜N、人数に応じて上限を動的に変更)
- 「グループ分け」ボタン

### 結果エリア
- グループごとにカード表示(グループ1、グループ2、...)
- 各カードにメンバー名を一覧表示
- 「リロール」ボタン(再シャッフル)
- 「リセット」ボタン(入力画面に戻る)

## ファイル構成

```
instant-group/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── src/
│   ├── main.tsx
│   ├── App.tsx              # メインコンポーネント
│   ├── App.module.css       # スタイル
│   ├── components/
│   │   ├── NameInput.tsx     # 名前入力 + リスト
│   │   ├── NameInput.module.css
│   │   ├── GroupResult.tsx   # グループ分け結果表示
│   │   └── GroupResult.module.css
│   └── utils/
│       └── shuffle.ts        # Fisher-Yatesシャッフルアルゴリズム
```

## 実装ステップ

1. **プロジェクト初期化**: Vite + React + TypeScript のセットアップ
2. **シャッフルユーティリティ作成**: Fisher-Yatesアルゴリズムでランダム分け
3. **NameInput コンポーネント**: 名前入力・追加・削除のUI
4. **GroupResult コンポーネント**: グループ分け結果の表示
5. **App コンポーネント**: 状態管理と画面遷移ロジック
6. **スタイリング**: レスポンシブ対応のCSS
7. **動作確認・コミット・プッシュ**

## ロジック詳細

### シャッフルアルゴリズム
- Fisher-Yates shuffle で名前配列をランダムに並べ替え
- 並べ替えた配列を指定グループ数に均等分配(余りは先頭グループから1人ずつ追加)

### バリデーション
- 名前が2人未満の場合はグループ分け不可
- グループ数は2以上、人数以下
- 空の名前は追加不可
- 重複名はそのまま許可(同姓同名対応)
